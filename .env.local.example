# URL da API backend — NUNCA expor secrets aqui
NEXT_PUBLIC_API_URL=https://api.japatreinador.com.br
NEXT_PUBLIC_APP_NAME=Japa Treinador

# Variáveis de servidor (não expostas ao browser)
# Usado apenas em Server Actions / Route Handlers
API_INTERNAL_URL=http://localhost:8000
